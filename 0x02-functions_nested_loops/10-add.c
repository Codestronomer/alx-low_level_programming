#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and return the result
 * @a : int type number
 * @b : int type number
 * Return: sum of arguments
 */
int add(int a, int b)
{
return (a + b);
}

