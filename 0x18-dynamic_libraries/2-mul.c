#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * mul - multiplies two integers
 * @a : one of the parameters to be multiplied
 * @b : one of the parameters to be multiplied
 * Return: result of the multiplication
 */

int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}

